<UserControl x:Class="MuliTask.Tasks.EditTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:custom="clr-namespace:MuliTask.Keyboard"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="725">

    <Viewbox Stretch="Uniform">
        <Grid Name="MainGrid" MinWidth="350" MinHeight="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="1.15*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <custom:KeyboardController x:Name="CustomKeyboardController" Grid.Row="1" />

        </Grid>
    </Viewbox>
</UserControl>
